# Monad testnet RPC endpoint
MONAD_RPC_URL=

# Throwaway wallet used by the agent to send rebalance transactions.
# Never use a wallet that holds real funds. Never commit the filled-in .env.
AGENT_PRIVATE_KEY=

# Deployed LeashVault address (populated after running the deploy script)
VAULT_ADDRESS=
